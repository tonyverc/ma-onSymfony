{% extends 'base.html.twig' %}
{% block title %}Chantier{% endblock %}

{% block body %}
<div class="container my-5 d-flex justify-content-center flex-column">
	<div class="card" style="width:auto;">
		<div class="card-header ">
			<h3>{{ chantier.nom }}</h3>
		</div>
            <img style ="max-height: 300px; object-fit: cover;" src="{{ asset('images/' ~ chantier.image) }}" class="card-img-top px-4 img-fluid" alt="...">
		<div class="card-body">
			<p class="card-text">{{chantier.description}}</p>
		</div>
    </div>
</div>

<div class ="d-flex justify-content-center align-items-center gap-5">
<a href="{{ path('app_chantier_index') }}" class="btn btn-primary">retour à la liste</a>

<a href="{{ path('app_chantier_edit', {'id': chantier.id}) }}" class="btn btn-primary">Modifier</a>
{{ include('chantier/_delete_form.html.twig') }}
</div>

{% endblock %}
